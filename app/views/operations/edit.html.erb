<div class="page-wrapper-sm">
  <h1>Modifier l'opération</h1>
  <p class="page-subtitle">Modifier les détails de l'opération</p>

  <div class="card card-elevated">
    <%= render "form" %>
  </div>

  <div class="back-link">
    <%= link_to "← Retour à la fiche", operation_path(@operation) %>
  </div>
</div>
